version: '3.8'
services:
  messenger-worker:
    build: .
    command: php bin/console messenger:consume async --limit=100 --memory-limit=256M
    restart: unless-stopped
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=postgresql://...
      - USE_TEST_DATA=false
    depends_on:
      - postgres