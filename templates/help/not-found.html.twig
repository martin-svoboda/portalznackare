{% extends 'base.html.twig' %}

{% block title %}{{ pageTitle|default(sectionTitle|default('Stránka nenalezena')) }} - Nápověda{% endblock %}

{% block body %}
<div class="section">
    <div class="container mx-auto">
        <div class="flex gap-8">
            {# Sidebar s navigací #}
            <aside class="w-64 flex-shrink-0">
                <div class="card">
                    <div class="card__header">
                        <h2 class="card__title">
                            <a href="{{ path('help_index') }}" class="text-inherit no-underline">📚 Nápověda</a>
                        </h2>
                    </div>
                    <div class="card__content p-0">
                        <nav class="nav nav--vertical">
                            {% for section in navigation %}
                                <div class="nav__section">
                                    <a href="{{ section.url }}" 
                                       class="nav__section-title {% if currentSection == section.name %}nav__section-title--active{% endif %}">
                                        <span class="nav__icon">{{ section.icon }}</span>
                                        {{ section.title }}
                                    </a>
                                    
                                    {% if section.pages and currentSection == section.name %}
                                        <ul class="nav__list">
                                            {% for page in section.pages %}
                                                <li class="nav__item">
                                                    <a href="{{ page.url }}" 
                                                       class="nav__link {% if currentPage is defined and currentPage == page.name %}nav__link--active{% endif %}">
                                                        {{ page.title }}
                                                    </a>
                                                </li>
                                            {% endfor %}
                                        </ul>
                                    {% endif %}
                                </div>
                            {% endfor %}
                        </nav>
                    </div>
                </div>
            </aside>
            
            {# Hlavní obsah #}
            <main class="flex-1">
                <div class="card">
                    <div class="card__header">
                        {# Breadcrumb navigace #}
                        <nav class="breadcrumb">
                            <a href="{{ path('help_index') }}" class="breadcrumb__link">Nápověda</a>
                            {% if currentSection is defined %}
                                <span class="breadcrumb__separator">›</span>
                                {% if currentPage is defined %}
                                    <a href="{{ path('help_section', {section: currentSection}) }}" class="breadcrumb__link">
                                        {{ sectionTitle }}
                                    </a>
                                    <span class="breadcrumb__separator">›</span>
                                    <span class="breadcrumb__current">{{ pageTitle|default('Stránka') }}</span>
                                {% else %}
                                    <span class="breadcrumb__current">{{ sectionTitle }}</span>
                                {% endif %}
                            {% endif %}
                        </nav>
                        
                        <h1 class="page-title">{{ pageTitle|default(sectionTitle|default('Připravujeme')) }}</h1>
                    </div>
                    <div class="card__content">
                        <div class="content text-center py-12">
                            <div class="text-6xl mb-6">🚧</div>
                            
                            <p class="text-lg text--muted mb-6">
                                {{ message|default('Tato stránka ještě není k dispozici.') }}
                            </p>
                            
                            <div class="alert alert--info inline-block text-left">
                                <div class="alert__content">
                                    <h4 class="alert__title">Máte konkrétní dotaz?</h4>
                                    <p>Neváhejte nás kontaktovat na <a href="mailto:support@kct.cz">support@kct.cz</a></p>
                                </div>
                            </div>
                            
                            <div class="mt-8">
                                <a href="{{ path('help_index') }}" class="btn btn--secondary">
                                    ← Zpět na hlavní stránku nápovědy
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
                
                {# Návrhy dalších stránek #}
                {% if navigation %}
                <div class="card mt-6">
                    <div class="card__header">
                        <h3 class="card__title">Možná hledáte</h3>
                    </div>
                    <div class="card__content">
                        <div class="grid grid-cols-2 gap-4">
                            {% for section in navigation|slice(0, 4) %}
                                {% if section.pages %}
                                    <div>
                                        <h4 class="font-medium mb-2">{{ section.icon }} {{ section.title }}</h4>
                                        <ul class="list list--links text-sm">
                                            {% for page in section.pages|slice(0, 3) %}
                                                <li><a href="{{ page.url }}">{{ page.title }}</a></li>
                                            {% endfor %}
                                        </ul>
                                    </div>
                                {% endif %}
                            {% endfor %}
                        </div>
                    </div>
                </div>
                {% endif %}
            </main>
        </div>
    </div>
</div>
{% endblock %}